# llm-hub
Aggregate multiple Ollama instances — plus LM Studio and any OpenAI-compatible backend — into a single unified API endpoint: route by model automatically, apply uniform authentication, fill in Ollama's missing Rerank API, and inject request parameters through aliases (e.g. disabling thinking mode).
